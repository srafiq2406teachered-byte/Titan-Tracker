import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App' // Letting Vite handle the extension automatically

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
